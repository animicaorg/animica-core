# Runtime dependencies (pinned)
fastapi==0.114.2
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
msgspec==0.18.6
cbor2==5.6.4
python-dotenv==1.0.1
structlog==24.4.0
prometheus-client==0.20.0
aiosqlite==0.20.0

# --- Monorepo locals (optional) ---
# If running inside the Animica repo, you can install the local SDK/VM packages in editable mode:
# pip install -e ../sdk/python
# pip install -e ../vm_py
