// All providers are now real adapters (see adapters.ts). The Animica internal
// worker provider is DB-backed and lives in the API layer
// (apps/api/src/providers/animica-worker.adapter.ts).
export {};
