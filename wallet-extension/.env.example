# ──────────────────────────────────────────────────────────────────────────────
# Animica Wallet Extension — .env.example
# Copy to `.env` and adjust for your setup. Values prefixed with VITE_ are
# exposed at build-time to the extension code via import.meta.env.*.
# ──────────────────────────────────────────────────────────────────────────────

# Node RPC endpoints (HTTP + optional WS). Used by background for JSON-RPC.
VITE_RPC_URL=http://127.0.0.1:8545/rpc
VITE_WS_URL=ws://127.0.0.1:8546

# Default chain the wallet will select on first run (overridable in UI).
VITE_CHAIN_ID=1337

# Network preset used by src/background/network/networks.ts (devnet|testnet|mainnet).
VITE_NETWORK_PRESET=devnet

# PQ crypto backend toggle for keygen/signing:
#  - auto  : try WASM, fall back to JS if needed
#  - wasm  : require WASM (fail if missing)
#  - js    : force JS (slow; good for debugging)
VITE_PQ_BACKEND=auto

# ── Dev server / CORS helpers (only affect `scripts/dev.ts` during local dev) ──
# Comma-separated list of web origins allowed to talk to the extension dev server.
# Typical values are your Vite app origins when working on studio-web or demos.
VITE_DEV_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# If you run a local RPC proxy to sidestep CORS during webapp development, put it here.
# Leave blank to talk to VITE_RPC_URL directly from the extension SW.
VITE_DEV_RPC_PROXY=

# Optional extra headers the dev proxy should add to RPC responses (comma-separated key:val).
# Example: VITE_DEV_PROXY_HEADERS=Access-Control-Allow-Origin:*,Access-Control-Allow-Headers:*
VITE_DEV_PROXY_HEADERS=

# Log level for background/service worker (debug|info|warn|error).
VITE_LOG_LEVEL=info
