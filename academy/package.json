{
  "name": "@animica/academy",
  "private": true,
  "version": "0.1.0",
  "description": "Animica Academy (academy.animica.org) — interactive tutorials, achievements, reward pool. Wallet-extension powered.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "concurrently \"pnpm -C api dev\" \"pnpm -C web dev\"",
    "build": "pnpm -C api build && pnpm -C web build",
    "test": "pnpm -C api test && pnpm -C web test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
