# __NAME__ contract

Scaffolded by `animica-agent contract scaffold __NAME__`.

- Deploy with your preferred toolchain (Foundry, Hardhat, or Animica's own contract pipeline).
- Update the constructor arguments and entry points to match your domain.
- Configure your RPC and chain id in `.animica/agent.json`.

## Local quickstart

```sh
animica-agent doctor
animica-agent rpc call eth_chainId
```
