{
  "name": "@animica/pool-web",
  "private": true,
  "version": "0.1.0",
  "description": "Animica mining-pool dedicated site (pool.animica.org). Lists Stratum endpoints, AICF model serving stats, hardware tier eligibility, miner downloads.",
  "type": "module",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 4322",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port 4322",
    "check": "astro check",
    "sync": "astro sync",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "astro": "^4.16.0",
    "@astrojs/tailwind": "^5.1.0",
    "tailwindcss": "^3.4.10"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/node": "^20.16.0",
    "typescript": "^5.6.0"
  }
}
