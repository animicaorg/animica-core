# Animica Wallet — .gitignore
# Notes:
# - We intentionally KEEP `.metadata` and `.vscode/` in the repo (tracked).
# - We also keep platform project files that are hand-edited in this repo layout.

# ----------------
# Flutter / Dart
# ----------------
.buildlog/
build/
.dart_tool/
.packages
.pub-cache/
.flutter-plugins
.flutter-plugins-dependencies
.melos_tool/

# Keep Flutter tool metadata tracked
!.metadata

# Coverage & temp
coverage/
*.tmp
*.temp
*.swp
*.swo
*.log

# Env files (keep example)
.env
.env.*
!.env.example

# ----------------
# IDEs / Editors
# ----------------
# JetBrains / Android Studio
*.iml
.idea/

# VS Code (keep project settings)
!.vscode/
.vscode/**.cache
.vscode/**.tmp

# ----------------
# Android
# ----------------
android/.gradle/
android/local.properties
android/key.properties
**/*.keystore
**/*.jks

# Gradle caches
.gradle/

# ----------------
# iOS / macOS (CocoaPods & Xcode build artefacts)
# ----------------
**/DerivedData/
**/Pods/
**/.symlinks/
**/Flutter/Flutter.framework
**/Flutter/Generated.xcconfig
**/Flutter/ephemeral/
**/*.xcuserstate
**/*.xcuserdatad
**/*.mode1v3
**/*.mode2v3
**/*.perspectivev3
# Xcode workspace is generated by CocoaPods; usually not committed
**/*.xcworkspace

# Codesigning secrets (do NOT commit)
**/*.p12
**/*.pem
**/*.mobileprovision
**/*.key

# ----------------
# Windows
# ----------------
# Build outputs and debug symbols
windows/runner/Debug/
windows/runner/Release/
windows/runner/*.pdb

# ----------------
# Linux
# ----------------
# (Do NOT ignore linux/flutter/generated_plugin_registrant.cc — we keep it tracked)
# Add any extra local build caches here if needed.

# ----------------
# Web
# ----------------
# Flutter builds go to build/ already
# Add additional web build caches here if needed.

# ----------------
# Fastlane (keep folder, ignore outputs)
# ----------------
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/
fastlane/test_output/

# ----------------
# Misc binaries/secrets
# ----------------
**/*.crt
**/*.der
**/*.cer
**/*.csr

