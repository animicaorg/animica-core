# Studio Web — environment variables (copy to .env and edit as needed)

# JSON-RPC endpoint for your node.
# Examples:
#  - Local devnet node: http://localhost:8545
#  - With HTTPS:        https://devnet.animica.local:8545
VITE_RPC_URL=http://localhost:8545

# Chain ID the app should target.
# From spec/chains.json: mainnet=1, testnet=2, devnet=1337
VITE_CHAIN_ID=1337

# Studio Services base URL (deploy/verify/faucet/simulate proxy).
# Leave blank to disable those features in the UI.
VITE_SERVICES_URL=http://localhost:8787
