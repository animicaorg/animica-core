---
title: "Mainnet readiness update"
description: "Core networking, RPC, and explorer pipelines are stable. Operators can now run full nodes with monitoring and status endpoints."
date: 2025-01-15
tags: ["mainnet", "ops", "rpc"]
---

We have completed the latest production readiness pass:

- RPC health checks now return consistent status signals.
- Explorer and Studio are aligned with the latest chain metadata.
- Node operators can rely on `animica node status` for fast health checks.

Next up: publishing pool endpoints and wallet downloads with signed manifests.
