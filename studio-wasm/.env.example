# ------------------------------------------------------------------------------
# @animica/studio-wasm — Environment Example
#
# Copy to `.env` (or `.env.local`) and tweak as needed. Variables here can be
# consumed by your bundler (Vite/webpack define plugin) or by the fetch scripts.
# ------------------------------------------------------------------------------

# Pin the exact Pyodide release (kept in sync with pyodide.lock.json)
PYODIDE_VERSION=0.24.1

# Toggle whether to fetch Pyodide assets from a CDN.
# If "false", the package will prefer vendored files under studio-wasm/vendor/.
# If "true", PYODIDE_CDN must be set to a base URL that hosts pyodide.{js,wasm,data}.
PYODIDE_USE_CDN=false

# Optional CDN base URL (used only when PYODIDE_USE_CDN=true).
# Common choices:
#   https://cdn.jsdelivr.net/pyodide/v0.24.1/full
#   https://cdn.jsdelivr.net/npm/pyodide@0.24.1
PYODIDE_CDN=https://cdn.jsdelivr.net/pyodide/v0.24.1/full

# Optional local/offline cache directory for Pyodide assets.
# If present, the loader/fetch script can read from here before hitting the network.
# Use an absolute path in production; relative works for local dev.
PYODIDE_OFFLINE_DIR=.cache/pyodide

# (Optional) Subresource Integrity (SRI) hashes for CDN-delivered assets.
# Leave blank to disable SRI verification. Recommended for production if using CDN.
# Examples shown are placeholders — replace with the real sha384/sha512 digests.
PYODIDE_SRI_JS=
PYODIDE_SRI_WASM=
PYODIDE_SRI_DATA=

# (Optional) Preload behavior — eagerly warm Pyodide on app startup.
# Used by your app's loader; not enforced by this package.
STUDIO_WASM_PRELOAD=true

# (Optional) Log level for the loader ("silent" | "error" | "warn" | "info" | "debug")
STUDIO_WASM_LOG_LEVEL=info
