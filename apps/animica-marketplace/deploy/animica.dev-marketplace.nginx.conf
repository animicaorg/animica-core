# Animica AI Marketplace — nginx location blocks for animica.dev
# ---------------------------------------------------------------------------
# APPLY: paste these blocks into /etc/nginx/sites-available/animica.dev.conf
# INSIDE the `server { ... }` that listens on 443, immediately AFTER the
# `location ~* \.(js|css|...)$ { ... }` regex asset block, then:
#     nginx -t && systemctl reload nginx
# A backup of the pre-marketplace config is at:
#     /root/site-backups/animica.dev.conf.pre-marketplace.YYYYMMDD
#
# Why ^~ : the existing regex asset location outranks plain prefix locations, so
# /marketplace/_next/*.js would 404 from disk. ^~ prefixes win over the regex.
# The HTML locations include the banner PROXY snippet (sets Accept-Encoding "" so
# the upgrade-banner sub_filter still matches the proxied Next.js responses).
# ---------------------------------------------------------------------------

location ^~ /_next/ {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    expires 1h;
}

# Internal-only endpoints: Bearer-gated AND intended solely for local daemons (the mascot posting
# engine, the newsletter sender) that call them on 127.0.0.1:4950 directly. Deny them at the public
# edge — a longer ^~ prefix / exact `=` match outranks the general /api/mkt/ block below. The Animal
# internal routes return UNSEALED social tokens, so this deny is a hard isolation boundary, not a
# nicety. apply-nginx.sh asserts these lines are present after applying.
location ^~ /api/mkt/v1/animal/internal/ { return 404; }
location = /api/mkt/v1/newsletter/confirmed { return 404; }

# GPU Studios (9.0.0) — large binary transfer routes. These are LONGER ^~ prefixes than
# /api/mkt/ below, so they win: only these three carry the big body/stream settings; the
# generic /api/mkt/ block keeps its 24m cap. (Also mirrored in anm-studios-9.0.0.nginx-snippet.conf.)
location ^~ /api/mkt/v1/media/uploads {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    client_max_body_size 512m;
    proxy_request_buffering off;
    proxy_buffering off;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
}
# 9.0.1: inline JSON result path used by classic-kind miners. Without this the
# /api/mkt/ catch-all's 24m cap 413s results over ~18MB binary while the app route
# accepts 48MB base64 — finished renders die at the edge.
location ^~ /api/mkt/v1/media/miner/result {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    client_max_body_size 64m;
    proxy_request_buffering off;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
}
location ^~ /api/mkt/v1/media/miner/result-file {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    client_max_body_size 1g;
    proxy_request_buffering off;
    proxy_read_timeout 600s;
    proxy_send_timeout 600s;
}
location ^~ /api/mkt/v1/media/artifacts/ {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_buffering off;
    proxy_read_timeout 600s;
}

# App Store — APK publishing uploads + entitlement-gated APK downloads.
# One block for the whole /store/ API: nginx cannot regex-scope inside a ^~ prefix
# tree (the ^~ /api/mkt/ block below suppresses regex locations entirely), so the
# longer ^~ prefix here is the only way to lift the 24m cap for
# POST /store/apps/<slug>/builds and to stream GET /store/download/<token>.
# proxy_buffering off + NO sub_filter/banner include: download bytes MUST match the
# catalog sha3 exactly (same rule as /anm/ hosted content and media artifacts).
location ^~ /api/mkt/v1/store/ {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    client_max_body_size 512m;
    proxy_request_buffering off;
    proxy_buffering off;
    proxy_read_timeout 600s;
    proxy_send_timeout 600s;
}

# Developer API — open to humans AND autonomous agents (JSON; no banner).
location ^~ /api/mkt/ {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 180s;
    client_max_body_size 24m;
}

location ^~ /embed/ {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

location ^~ /marketplace {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    include /etc/nginx/snippets/anm-upgrade-inject-proxy.conf;
}

location ^~ /studio {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    include /etc/nginx/snippets/anm-upgrade-inject-proxy.conf;
}

location ^~ /my-ai {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    include /etc/nginx/snippets/anm-upgrade-inject-proxy.conf;
}

location ^~ /names {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    include /etc/nginx/snippets/anm-upgrade-inject-proxy.conf;
}

# Animica Animal operator console (8.2.0). Its Bearer-gated internal engine routes
# stay 404'd at the edge by the longer-prefix /api/mkt/v1/animal/internal/ deny above.
location ^~ /animal {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    include /etc/nginx/snippets/anm-upgrade-inject-proxy.conf;
}

# ── Hire Me managed services (2026-07-26) ─────────────────────────────────────
# Public services page + customer dashboard on the apex; admin console reuses ^~ /admin.
location ^~ /hire {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    include /etc/nginx/snippets/anm-upgrade-inject-proxy.conf;
}
location ^~ /dashboard {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
# Subdomain server blocks: see /etc/nginx/sites-enabled/hire.animica.dev.conf
# (admin.animica.dev → /admin/hire, dashboard.animica.dev → /dashboard; certbot after DNS).

# ── Platform subscriptions + Animica Workers (2026-08-01, docs/subscriptions.md) ──
# /pricing (tier cards + PayPal checkout), /settings/* (billing dashboard), /workers
# (Worker console). The PayPal webhook posts to /api/mkt/v1/billing/paypal/webhook,
# covered by the existing ^~ /api/mkt/ proxy block.
location ^~ /pricing {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    include /etc/nginx/snippets/anm-upgrade-inject-proxy.conf;
}
location ^~ /settings {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
location ^~ /workers {
    proxy_pass http://127.0.0.1:4950;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    include /etc/nginx/snippets/anm-upgrade-inject-proxy.conf;
}
