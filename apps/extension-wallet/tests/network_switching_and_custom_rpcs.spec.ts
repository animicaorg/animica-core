import { describe, it } from 'vitest'

// Vertical 8: Network switching, metadata propagation, and custom RPC lifecycle
// These placeholders capture expected UX + provider state transitions.

describe('network switching and custom RPCs', () => {
  it.todo('switches between mainnet, testnet, and devnet while updating provider.chainId')
  it.todo('persists user-selected network across sessions and rehydrates on load')
  it.todo('adds custom RPC endpoints with validation and removes them cleanly')
  it.todo('updates provider metadata and notifies dapps when networks change')
  it.todo('handles RPC failures gracefully and falls back to last known good network')
})
