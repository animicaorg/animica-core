# Build artifacts & caches — never committed.
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# PyInstaller / packaging output (regenerated by the build).
build/
dist/
build-win/
dist-win/

# Local/dev runtime state (demo wallet fixture, not source).
wallet_state.json

# OS cruft
.DS_Store
