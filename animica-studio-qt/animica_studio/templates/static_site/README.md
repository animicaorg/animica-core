# {{name}}

A plain **HTML / CSS / JS** static site generated by Animica Studio Qt.

## Run

Open `index.html` directly, or serve the folder:

```bash
python -m http.server 8080
```

Then visit http://localhost:8080
