# {{name}}

A minimal **Express.js** HTTP API generated by Animica Studio Qt.

## Install

```bash
npm install
```

## Run

```bash
npm start
```

Endpoints: `GET /`, `GET /health`, `POST /echo` (JSON `{ "message": "..." }`).
