# Animica AICF provider (FastAPI) — runtime requirements
# Tip: for local dev, create a virtualenv then `pip install -r requirements.txt`.

fastapi>=0.111
uvicorn[standard]>=0.27
pydantic>=2.5
httpx>=0.27
python-dotenv>=1.0
msgspec>=0.18
orjson>=3.10
structlog>=24.1
prometheus-client>=0.20
tenacity>=8.2
anyio>=4.4
typing-extensions>=4.9

# --- Optional add-ons ---------------------------------------------------------
# Uncomment the groups you need in your environment.

# Attestation / evidence parsing (used by app/compute/attestation.py)
# cryptography>=42.0
# cbor2>=5.6.3
# pycose>=1.0.1

# Observability extras (metrics + traces)
# prometheus-fastapi-instrumentator>=6.1.0
# opentelemetry-sdk>=1.26
# opentelemetry-exporter-otlp>=1.26

# GPU backends (choose one; platform-specific)
# cupy-cuda12x>=12.3 ; platform_system=="Linux"
# cupy-rocm-5-7>=12.3 ; platform_system=="Linux"
# pyopencl>=2024.1

# --- Dev / test toolchain (optional) -----------------------------------------
# pytest>=8.0
# pytest-asyncio>=0.23
# pytest-cov>=5.0
# ruff>=0.5.0
# mypy>=1.7
# types-requests>=2.31.0.20240218
# pytest-httpx>=0.30
# coverage>=7.5
