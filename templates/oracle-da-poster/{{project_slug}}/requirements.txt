# Runtime dependencies for the Oracle DA Poster service.
# Install with: pip install -r requirements.txt

# Web framework & ASGI server helpers
fastapi>=0.111.0,<1
uvicorn[standard]>=0.30.0,<1

# Data validation / settings
pydantic>=2.6.0,<3
python-dotenv>=1.0.1,<2

# Networking / serialization
httpx>=0.27.0,<1
msgspec>=0.18.6,<1

# Metrics
prometheus-client>=0.20.0,<1

# Animica SDK (Python) — package name on PyPI may be "omni-sdk"
# If developing locally, replace the next line with a local path or VCS URL, e.g.:
# omni-sdk @ file:///ABSOLUTE/PATH/TO/sdk/python
omni-sdk>=0.1.0

# Optional performance extras (safe to comment out on Windows)
uvloop>=0.19.0,<1 ; sys_platform != "win32" and platform_python_implementation == "CPython"

# ------------- Developer tools (uncomment for local dev) -------------
# ruff>=0.5.0
# mypy>=1.10.0
# pytest>=8.2.0
# pytest-asyncio>=0.23.0
# pytest-cov>=5.0.0
# anyio>=4.4.0
# types-requests>=2.32.0.20240712
