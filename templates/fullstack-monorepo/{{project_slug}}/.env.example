# ==============================================================================
# .env.example — copy to .env and customize for your environment
#
# This file feeds BOTH:
#   • contracts/ tooling (Python) for building & deploying
#   • dapp/ (Vite + React) for connecting to the chain in the browser
#
# Notes:
#   - Lines starting with '#' are comments.
#   - Empty values mean "feature disabled" or "use default".
#   - For secrets (mnemonic / private key), NEVER commit your real values.
#   - If both DEPLOYER_PRIVATE_KEY and DEPLOYER_MNEMONIC are set, PRIVATE_KEY wins.
#   - In the dapp, only variables prefixed with VITE_ are exposed to the browser.
# ==============================================================================

# ------------------------------------------------------------------------------
# Core chain connection (used by deploy scripts and as defaults for the dapp)
# ------------------------------------------------------------------------------
RPC_URL=http://127.0.0.1:8645
# Example public devnet might look like:
# RPC_URL=https://rpc.devnet.animica.example

# Numeric chain id. 31337 is a common local/dev default.
CHAIN_ID=31337

# ------------------------------------------------------------------------------
# Deployer credentials (used by contracts deployment scripts)
# Provide ONE of these. PRIVATE_KEY takes precedence if both are set.
# ------------------------------------------------------------------------------
# A hex private key with 0x prefix (PREFER a dedicated throwaway key for dev):
DEPLOYER_PRIVATE_KEY=
# OR a BIP-39 mnemonic phrase. ACCOUNT_INDEX selects which child account to use.
DEPLOYER_MNEMONIC=
ACCOUNT_INDEX=0

# Optional: default gas/fee controls for deployment (leave blank for auto)
MAX_FEE_PER_GAS=
MAX_PRIORITY_FEE_PER_GAS=
GAS_LIMIT=

# ------------------------------------------------------------------------------
# Dapp web app configuration (Vite/React). These are read by the UI at runtime.
# If left empty, many UIs will fall back to sensible defaults, but it's best
# to set them explicitly to match RPC_URL/CHAIN_ID above after deployment.
# ------------------------------------------------------------------------------
VITE_RPC_URL=
VITE_CHAIN_ID=
# Set after running scripts/deploy_and_link.sh (the script can write this):
VITE_CONTRACT_ADDRESS=

# Optional: turn on verbose provider logs in the browser console (true/false)
VITE_DEBUG_PROVIDER=false

# ------------------------------------------------------------------------------
# Optional service endpoints (uncomment and set as needed)
# ------------------------------------------------------------------------------
# Studio services proxy (for source verification, AICF, etc.)
# VITE_STUDIO_SERVICES_URL=https://services.devnet.animica.example

# Explorer API base (used by the dapp to link out / fetch summaries)
# VITE_EXPLORER_API_URL=https://explorer.devnet.animica.example/api

# ------------------------------------------------------------------------------
# Indexer-lite / telemetry (if you run the local indexer template)
# ------------------------------------------------------------------------------
# INDEXER_RPC_URL=$RPC_URL
# INDEXER_DB_URL=sqlite:///./indexer.db
# INDEXER_BATCH_SIZE=200
# INDEXER_START_HEIGHT=0

# ------------------------------------------------------------------------------
# Advanced: CORS / origins hints (some proxies honor these envs)
# ------------------------------------------------------------------------------
# CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# ------------------------------------------------------------------------------
# Hints:
#   1) Copy this file to ".env" at the repo root.
#   2) Start a local node or export a public RPC.
#   3) Set DEPLOYER_* to a funded dev account.
#   4) Run ./scripts/build_all.sh then ./scripts/deploy_and_link.sh
#   5) The deploy script will write VITE_* values for you if possible.
# ------------------------------------------------------------------------------
