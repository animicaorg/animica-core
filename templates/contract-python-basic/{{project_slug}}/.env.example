# ==============================================================================
# {{project_slug}} — local environment (example)
# ------------------------------------------------------------------------------
# Copy to ".env" and adjust for your machine. This file is safe to commit;
# DO NOT commit the real ".env" that contains secrets.
#
# Used by tooling (sdk/python, vm_py, and template scripts) to connect to a node,
# sign transactions from a developer wallet, and (optionally) talk to studio-services.
# ==============================================================================

# --- Chain / Node ----------------------------------------------------------------
# HTTP JSON-RPC endpoint of your node (devnet or local)
RPC_URL=http://127.0.0.1:8545

# Optional WebSocket endpoint (subscriptions, live heads)
WS_URL=ws://127.0.0.1:8546

# Chain identifier (see spec/chains.json; devnet=1337, testnet=2, mainnet=1)
CHAIN_ID=1337

# Request timeout (seconds) for RPC calls
RPC_TIMEOUT_SECS=30

# --- Studio Services (optional) --------------------------------------------------
# If you run studio-services alongside the node, set its base URL here to enable
# source verification, faucet drip (dev only), and artifact hosting.
# Leave empty to skip those features.
SERVICES_URL=http://127.0.0.1:8080

# --- Deployer wallet -------------------------------------------------------------
# A mnemonic used only for local development. DO NOT reuse in production.
# Example value shown below is a placeholder—replace with your own throwaway phrase.
# The tooling derives Dilithium3/SPHINCS+ keys deterministically via HKDF-SHA3.
DEPLOYER_MNEMONIC="abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about"

# Optional: derivation index if you keep multiple accounts per mnemonic
ACCOUNT_INDEX=0

# Optional: bech32m address override (if you prefer to set an explicit address).
# When set, tooling will validate it matches the DEPLOYER_MNEMONIC at ACCOUNT_INDEX.
#DEPLOYER_ADDRESS=anim1qqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqqq0u4x

# --- Fees / Gas (tooling hints) --------------------------------------------------
# Minimum tip (in the chain's base-unit per gas) the deploy scripts should target.
# Leave blank to let the node's fee market suggest one.
#MIN_TIP_PER_GAS=0

# Absolute cap the deploy scripts will not exceed (protection against spikes).
#MAX_FEE_PER_GAS=100000

# --- Build / Deploy behavior toggles --------------------------------------------
# Whether to run a read-only simulation before sending a state-changing tx.
PREDEPLOY_SIMULATE=true

# Whether to call studio-services to verify source after a successful deploy.
VERIFY_AFTER_DEPLOY=false

# Emit extra debug logs from the scripts (INFO|DEBUG|WARNING|ERROR)
LOG_LEVEL=INFO

# --- Data Availability (optional; for examples using blobs) ---------------------
#DA_NAMESPACE=24
#DA_CLIENT_URL=http://127.0.0.1:8787

# --- AICF / Capabilities (optional; for AI/Quantum examples) --------------------
#AICF_PREFER_PROVIDER_ID=
#AICF_MAX_UNITS=1000

# Notes:
# - Create ".env" from this file and adjust values:
#     cp templates/contract-python-basic/{{project_slug}}/.env.example templates/contract-python-basic/{{project_slug}}/.env
# - Never commit ".env"; the template's .gitignore already excludes it.
# - For public devnet defaults, see tests/devnet/env.devnet.example or ops/.env.example.
