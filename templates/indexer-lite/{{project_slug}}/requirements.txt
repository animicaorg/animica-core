# ------------------------------------------------------------------------------
# {{ project_slug }} — Lightweight Animica Indexer
#
# Usage:
#   python -m venv .venv && source .venv/bin/activate
#   pip install --upgrade pip
#   pip install -r requirements.txt
#
# Notes:
# - This file mirrors the core deps from pyproject.toml for simple installs.
# - Optional stacks (API server, PostgreSQL) are provided as commented lines.
# - Keep versions aligned with pyproject to avoid "works in dev, breaks in prod".
# ------------------------------------------------------------------------------

##########
# CORE RUNTIME (required)
##########
httpx>=0.27.0                 # JSON-RPC over HTTP with retries
websockets>=12.0              # WS subscriptions (newHeads, pendingTxs)
anyio>=4.0.0                  # async concurrency backend
pydantic>=2.5.0               # models (Blocks/Tx/Head) & validation
SQLAlchemy>=2.0.25            # SQL persistence (SQLite/Postgres)
alembic>=1.13.0               # schema migrations
typer>=0.9.0                  # CLI (backfill, live tail, admin)
prometheus-client>=0.17.0     # /metrics exporter
python-dotenv>=1.0.0          # load .env for config
tenacity>=8.2.3               # robust retries for RPC/DB ops
orjson>=3.9.10                # fast JSON encode/decode
structlog>=24.1.0             # structured logs
uvloop>=0.19.0; platform_system != "Windows"  # faster event loop (non-Windows)

##########
# OPTIONAL: READ-ONLY HTTP API (uncomment to enable /api)
##########
# fastapi>=0.111.0            # lightweight query API (blocks/tx/events)
# uvicorn>=0.30.0             # ASGI server

##########
# OPTIONAL: POSTGRES DRIVER (choose one)
##########
# psycopg[binary]>=3.1.18     # easy install, good default
# psycopg[c]>=3.1.18          # libpq-backed, faster if system libs available

##########
# OPTIONAL: DEV / TEST TOOLING (usually placed in a separate dev-requirements.txt)
##########
# pytest>=8.2.0
# pytest-asyncio>=0.23.0
# pytest-cov>=5.0.0
# ruff>=0.5.0
# mypy>=1.10.0
# types-requests>=2.32.0.20240622

# End of file
